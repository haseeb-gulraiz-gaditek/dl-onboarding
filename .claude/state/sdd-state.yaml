# SDD State - Spec-Driven Development tracking
# Location: .claude/state/sdd-state.yaml
#
# This file tracks the cycle state for STD-001.
# Managed by /sdd/* commands. Do not edit by hand.

installed_standard_version: "1.5"
installed_at: "2026-04-30T20:36:01+05:00"
tracker_preference: "local"  # "local" | "linear" | "github"
current_cycle:
  slug: null
  phase: null                # proposal | implement | complete
  started_at: null
  tracker_ref: null
  base_branch: null
  branch_name: null
history:
  - slug: my-tools-explore-new-tabs
    completed: "2026-05-03"
    summary: "/tools page backing across 3 tabs. New user_tools collection with source-precedence rule baked into upserts (explicit_save never demoted). Six endpoints behind require_role(user): POST/DELETE/PATCH/GET /api/me/tools (mine), GET /api/tools (explore — tools_seed only), GET /api/launches (new — default-filtered to joined communities; ?all=true escape hatch). Auto-populate hook on POST /api/answers fires only for multi_select with slug-valued options; resolves through both tool collections via the new shared find_tool_anywhere helper (which also replaces cycle #9's inline copy in engine.py). Constitutional separation enforced at the route layer: /api/tools sealed against tools_founder_launched, /api/launches sealed against tools_seed. 31 tests; full suite 238 green."
  - slug: launch-publish-and-concierge-nudge
    completed: "2026-05-03"
    summary: "Activates the C2 amendment from cycle #8. Three new endpoints (GET /r/{id} unauth redirect with HMAC user-hash; GET /api/tools/{slug} canonical product page reading both tool collections; POST /api/concierge/respond for tell_me_more/skip on nudges). Three modified endpoints (POST /api/founders/launch gains target_community_slugs 1..6 required; POST /admin/launches/{id}/approve triggers publish_launch orchestrator; POST /api/recommendations gains launches[] slot). Synchronous publish_launch runs 5 steps in admin approve: embed ICP, embed founder-launched tool, fan out posts to all target communities (attached_launch_id set), scan profiles for top-5 by similarity → concierge_nudge notifications + cache bust, return summary. Per-step exceptions logged and swallowed. Recommendation engine runs parallel similarity_search over tools_founder_launched (top-5, no LLM ranker call — keeps the 'founder payments never move organic ranking' invariant intact via separate ranking pass). New engagements collection (CPA-ready event log; write-only in V1). 22 tests; full suite 207 green. Three learnings (third monkey-patch site for embed_text, sync orchestrator pattern with bounded fan-out, constitutional test pattern asserting structural separation)."
  - slug: founder-launch-submission-and-verification
    completed: "2026-05-02"
    summary: "Founder side opens. Three new collections (launches, tools_founder_launched, notifications) + 7 endpoints (3 founder, 4 admin). Submission via POST /api/founders/launch behind require_role(founder). Admin queue/approve/reject reuses cycle #3's require_admin (ADMIN_EMAILS). Approve derives slug from product URL host (kebab-cased, full TLD), scans both tool collections for collision, creates tools_founder_launched row with source='founder_launch'+is_founder_launched=true, atomic state transition via find_one_and_update on verification_status='pending'. Notifications collection writes launch_approved/rejected rows (cycle #11 builds inbox UI; SMTP deferred). Pre-cycle C2 amendment to principles.md We Always #3: launches MAY appear alongside organic recs in a separate slot, gated by match-quality threshold; cycle #9 implements the fan-in. 31 tests; full suite 185 green."
  - slug: communities-and-flat-comments
    completed: "2026-05-02"
    summary: "Six new collections (communities, memberships, posts, comments, votes) + tools_seed.vote_score field. Nine endpoints across communities/posts/comments/votes routers. Constitutional founder-write-block enforced via require_role(user) on five write endpoints; reads open to founders. Single canonical post + cross_posted_to[] (≤3 communities) keeps vote tallies coherent. 10 hand-authored seed communities across role/stack/outcome axes. Vote semantics: insert/toggle-off/flip with denormalized vote_score on posts, comments, AND tools (drives product-page like count). 35 tests; full suite 154 green."
  - slug: recommendation-engine
    completed: "2026-05-02"
    summary: "POST /api/recommendations behind require_role(user). 3-answer gate; 7-day cache (TTL + last_invalidated_at). Pipeline: ensure_profile_embedding → similarity_search top-20 (curation_status=approved) → GPT-5 ranker via beta.chat.completions.parse with Pydantic structured output → drop hallucinated slugs → upsert. verdict ∈ {try, skip} surfaces 'Recommend honestly, including skip-this' principle. Degraded path on ranker exception caches a similarity-only response with verdict=try. 18 tests added; full suite 119 green."
  - slug: auth-role-split
    completed: "2026-04-30"
    summary: "FastAPI + MongoDB auth surface with non-transferable role_type (user|founder), JWT (HS256, header transport), bcrypt, role-aware middleware, 19 tests."
  - slug: question-bank-and-answer-capture
    completed: "2026-05-01"
    summary: "questions/answers/profiles collections + GET /api/questions/next + POST /api/answers behind require_role(user). 12 hand-authored seed questions across 6 categories. Lazy profile creation; founders structurally barred. 21 tests."
  - slug: catalog-seed-and-curation
    completed: "2026-05-01"
    summary: "tools_seed collection + 547-entry catalog from outsourced research agent. python -m app.seed catalog idempotent loader with founder-launched protection. Admin curation UI (list, approve, reject-with-comment) behind ADMIN_EMAILS env allowlist (no role_type amendment). 22 tests."
  - slug: weaviate-pipeline
    completed: "2026-05-02"
    summary: "OpenAI text-embedding-3-small + Weaviate Cloud Sandbox + Mongo (source of truth). Mid-cycle pivot from Atlas Vector Search to Weaviate per user call. Three CLIs: init-weaviate, backfill-tools, republish-tools. similarity_search with three execution paths (mongomock test fallback, Weaviate production, Mongo cosine production fallback). 546 tools embedded + published. 25 tests added; full suite 87 green."
  - slug: fast-onboarding-match-and-graph
    completed: "2026-05-02"
    summary: "POST /api/onboarding/match (API only; React graph is parallel Claude Design track). Two-mode dispatch by answered_count: generic role-bucket for Q1-3 (no OpenAI; ROLE_TO_CATEGORIES map; alphabetical top-5 with catalog-wide fallback), embedding mode for Q4+ (ensure_profile_embedding + similarity_search). OpenAI/Weaviate failures gracefully degrade to generic mode. 14 tests added; full suite 101 green."
